#include<iostream>#include<string>#include "Directory.h"using namespace std;int main(){    string start;    string arg1;    string arg2;    int argInt;    int argInt2;    char argChar;    Directory ourDirectory;    while (cin >> start)    {        if (start == "import")        {		//cout << "here";            cin >> arg1;            cin >> arg2;            ourDirectory.DirectoryImport(arg1, arg2);           // we callin import        }                else if (start == "remove")        {		cin >> (arg1) ;		ourDirectory.remove(arg1) ;            cout << endl;            //be steady calling remove        }        else if (start == "list")        {		            ourDirectory.list() ;        }        else if (start == "cat")        {            cin >> arg1;            ourDirectory.cat(arg1) ;            cout << endl;        }        else if (start == "display")        {            cin >> arg1;            cin >> argInt;            cin >> argInt2;            ourDirectory.Display(arg1, argInt, argInt2) ;            cout << endl;        }                else if (start == "overwrite")        {            cin >> arg1;            cin >> argInt;            cin >> argInt2;            cin >> argChar;            ourDirectory.Overwrite(arg1, argInt, argInt2, argChar);            cout << endl;                    }        else if (start == "clean")        {            cin >> argInt;            ourDirectory.Clean(argInt);        }        else if (start == "exit")        {            ourDirectory.exitD() ;        }        else if (start == "checkpoint")        {            for (int i = 0; i < 40; i++)            {                memcpy(&argInt, ourDirectory.CPR.bytes + 4*i, 4);                cout << "CPR "<< i << ": " << argInt << "\t";            }            for (int i = 161; i < 193; i++)            {                cout << "Live Segment " << i-161 << ": " << ourDirectory.CPR.bytes[i] << "\t";            }                        cout <<endl;        }        else if (start == "segsum")        {            cin >> argInt;            for (int i = 0; i < 1024; i++)            {                cout << "SEGSUM " << i <<": " << ourDirectory.Drive[argInt].SegSum.locArray[i] << "\t";            }                        cout <<endl;        }        else if(start == "imap"){                        for( int i = 0 ; i < 256*40 ; i++ ){                if (ourDirectory.imapFull[i] != 0)                {                    cout <<"IMAP "<< i << ": " << ourDirectory.imapFull[i] << "\t";                }            }            cout <<endl;        }    }        //Supports cat, display, and overwitre eventually    /*==== OUTPUTS ====*/}